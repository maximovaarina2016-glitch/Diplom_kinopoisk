# Онлайн кинотеатр Кинопоиск — ваш проводник в мир любимого кино, ярких историй и захватывающих сюжетов.
# ссылка - https://www.kinopoisk.ru

## Стек:
- pytest
- selenium
- requests
- allure
- config

## Структура:
- ./tests - тесты
- ./pages - описание страниц для UI-тестов
- ./api_pages - описание страниц для API-тестов

## Полезные ссылки
- [Документация API](https://kinopoiskapiunofficial.tech/documentation/api/)

# UI-тесты онлайн кинотеатра Кинопоиск

### Проект содержит автоматизированные тесты для проверки UI-функционала сайта [Кинопоиск](https://www.kinopoisk.ru/).
Используется паттерн Page Object Model (POM), фреймворк PyTest и генератор отчетов Allure Report.

# API-тесты онлайн кинотеатра Кинопоиск
### Проект содержит автоматизированные тесты для проверки API-функционала сайта [Кинопоиск](https://www.kinopoisk.ru/).

## Инструкция по запуску тестов для формирования отчета Allure

1. Установите необходимые зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Запустите тесты с генерацией отчета Allure:
   ```bash
   pytest --alluredir=./allure-results
   ```
   Эта команда запустит все тесты и сохранит результаты в директорию `./allure-results`.

---
## Инструкция по просмотру сформированного отчета Allure

1. После завершения тестов, чтобы просмотреть отчет, выполните команду:
   ```bash
   allure serve ./allure-results
   ```

   Эта команда запустит локальный сервер и откроет отчет в браузере.

## В отчете Allure вы увидите:
   - **Название теста**: Например, `Ввести название фильма`.
   - **Описание теста**: Описание, что тест проверяет корректность работы ввода названия.
   - **Шаги теста**:
     - Открытие страницы Кинопоиска.
     - Ввод названия фильма в поисковую строку.
     - Нажатие кнопки поиска.
     - Ожидание результата.
   - **Результат**: Успешное выполнение или ошибка с указанием ожидаемого и фактического результата.

### Библиотеки
- pip install pytest
- pip install selenium
- pip install webdriver-manager
- pip install allure-pytest

